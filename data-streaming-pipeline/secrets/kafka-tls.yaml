apiVersion: v1
kind: Secret
metadata:
  name: kafka-tls-secret
  namespace: streaming
type: Opaque
data:
  # Replace these values with Base64-encoded keys and certificates
  keystore.jks: <BASE64_ENCODED_KEYSTORE>
  truststore.jks: <BASE64_ENCODED_TRUSTSTORE>
